import { RestaurantDirective } from './restaurant.directive';

describe('RestaurantDirective', () => {
  it('should create an instance', () => {
    const directive = new RestaurantDirective();
    expect(directive).toBeTruthy();
  });
});
